# pegasuspad identity — placeholder.
# No pegasuspad email yet, so this file intentionally sets nothing and the global
# default (sean@skleinjung.com) applies. Add a [user] email here when one exists,
# and `[include] path = require-signing.inc` if these repos should force signing.
